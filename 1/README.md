

```
<input type="text" class="form-control  form-control Wdate" onClick=“Wdate Picker()”>
```

- `form-control Wdate ` 是控制图标
- 使用方法：
  - 1、在`input`中加入类名`class="form-control Wdate"`;添加点击事件调用函数`onClick="Wdate Picker()"`
  - 2、引入js包<script src="WdatePicker.js" &gt;<script&gt;

